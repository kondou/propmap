.gitignore export-ignore
.gitattributes export-ignore

# メンテナ専用（構築済みデータのリリース公開用。エンドユーザーには不要）
make_data_release.py export-ignore

# 開発時のコード変更検証専用（エンドユーザーには不要）
verify.sh export-ignore
verify.bat export-ignore

# アイコン原本（各HTMLへはdata URIで埋め込み済みのため、配布zipには不要）
# icons単体の行はディレクトリエントリ自体の除外用（gitattributesは再帰マッチしない）
icons export-ignore
icons/** export-ignore
